# Database Migration Governor

Use before VIL persistence, audit storage, schema, index, or tenant data changes.

Return forward plan, rollback plan, data risk, validation queries, and verification commands.
