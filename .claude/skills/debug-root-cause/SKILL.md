# Debug Root-Cause Investigator

Use for failing VIL tests, API errors, dashboard issues, or regressions.

Reproduce first, isolate root cause, make the smallest fix, and verify with focused commands.
